diff --git a/python/CMake_Macros.txt b/python/CMake_Macros.txt
index 45a8364..95b1fe8 100644
--- a/python/CMake_Macros.txt
+++ b/python/CMake_Macros.txt
@@ -23,14 +23,16 @@ function(create_symlinks source_folder dest_folder)
     file(REMOVE "${dest_folder}/${path_file}")
 
     # Get OS dependent path to use in `execute_process`
-    file(TO_NATIVE_PATH "${dest_folder}/${path_file}" link)
-    file(TO_NATIVE_PATH "${source_folder}/${path_file}" target)
+    set(link "${dest_folder}/${path_file}")
+    set(target "${source_folder}/${path_file}")
 
     # cmake-format: off
     if(UNIX)
-      set(command ln -s ${target} ${link})
+      set(command ln -s "${target}" "${link}")
     else()
-      set(command cmd.exe /c mklink ${link} ${target})
+      file(TO_NATIVE_PATH "${link}" link)
+      file(TO_NATIVE_PATH "${target}" target)
+      set(command cmd.exe /c mklink "${link}" "${target}")
     endif()
     # cmake-format: on
 
